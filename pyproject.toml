[project]
name = "automat"
version = "0.1.0"
description = "Local composition descriptor search for experimental band-gap regression."
requires-python = ">=3.10,<3.12"
dependencies = [
    "numpy",
    "pandas",
    "pymatgen",
    "ruamel.yaml",
    "scikit-learn",
]
